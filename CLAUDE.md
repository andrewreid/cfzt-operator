# CLAUDE.md

Repo handbook is `AGENTS.md` (platform-agnostic — Codex, Claude Code, other agents all read it).

RTK reference, bootstrap, delegation policy, code rules, reconciliation invariants, scope boundaries → see `AGENTS.md`.

Architecture + binding decisions → see `spec.md`.

Implementation plan → see `docs/plan.md` (created at first scaffold).
