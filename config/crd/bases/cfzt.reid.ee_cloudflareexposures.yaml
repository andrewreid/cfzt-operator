---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: cloudflareexposures.cfzt.reid.ee
spec:
  group: cfzt.reid.ee
  names:
    kind: CloudflareExposure
    listKind: CloudflareExposureList
    plural: cloudflareexposures
    shortNames:
    - cfe
    singular: cloudflareexposure
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.hostname
      name: Hostname
      type: string
    - jsonPath: .spec.tunnelRef.name
      name: Tunnel
      type: string
    - jsonPath: .spec.access.enabled
      name: Access
      type: boolean
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CloudflareExposure is the Schema for the cloudflareexposures
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CloudflareExposureSpec defines the desired state of CloudflareExposure.
            properties:
              access:
                default: {}
                description: AccessSpec controls Cloudflare Access protection.
                properties:
                  enabled:
                    default: false
                    type: boolean
                  policyRef:
                    description: AccessPolicyRef points at an existing Cloudflare
                      Access policy.
                    properties:
                      uuid:
                        pattern: ^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$
                        type: string
                    type: object
                type: object
              displayName:
                maxLength: 120
                type: string
              hostname:
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)+$
                type: string
              origin:
                description: OriginSpec describes the origin cloudflared should proxy
                  to.
                properties:
                  host:
                    minLength: 1
                    type: string
                  port:
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  protocol:
                    enum:
                    - http
                    - https
                    type: string
                type: object
              sourceRef:
                description: SourceRef identifies a same-namespace source object used
                  for defaulting.
                properties:
                  apiVersion:
                    type: string
                  kind:
                    enum:
                    - Service
                    - HTTPRoute
                    minLength: 1
                    type: string
                  name:
                    minLength: 1
                    type: string
                required:
                - kind
                - name
                type: object
              tunnelRef:
                description: TunnelRef identifies the CloudflareTunnel that publishes
                  this exposure.
                properties:
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
            required:
            - tunnelRef
            type: object
            x-kubernetes-validations:
            - message: origin protocol, host, and port are required unless sourceRef.kind
                is Service
              rule: (has(self.sourceRef) && self.sourceRef.kind == 'Service') || (has(self.origin)
                && has(self.origin.protocol) && has(self.origin.host) && has(self.origin.port))
            - message: hostname is required unless sourceRef.kind is HTTPRoute
              rule: has(self.hostname) || (has(self.sourceRef) && self.sourceRef.kind
                == 'HTTPRoute')
            - message: access.policyRef.uuid is required when access.enabled is true
              rule: '!has(self.access) || !self.access.enabled || (has(self.access.policyRef)
                && has(self.access.policyRef.uuid) && size(self.access.policyRef.uuid)
                > 0)'
          status:
            description: CloudflareExposureStatus defines the observed state of CloudflareExposure.
            properties:
              cloudflare:
                description: ExposureCloudflareStatus records Cloudflare-side resources
                  for one exposure.
                properties:
                  accessApplicationId:
                    type: string
                  dnsRecordId:
                    type: string
                  publicHostnameRouteHash:
                    type: string
                type: object
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedTunnelUid:
                description: |-
                  UID is a type that holds unique ID values, including UUIDs.  Because we
                  don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                  intent and helps make sure that UIDs and names do not get conflated.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
